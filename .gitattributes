# Auto detect text files and perform LF normalization
* text=auto

# The Gradle wrapper must keep its own line endings to stay executable.
gradlew text eol=lf
gradlew.bat text eol=crlf
gradle/wrapper/gradle-wrapper.jar binary
